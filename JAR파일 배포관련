작업이 실행중일 때 기존 바이너리에 영향이 있으면 오류 발생
1. MV 명령어는 파일을 이동시키는 명령어(파일 이름,위치만 변경하고 파일의 내용은 변경하지 않습니다.) JVM이 해당 클래스를 사용중이더라도 경로가 변경되었지만 파일 자체는 여전히 메모리에 로드되어 있음
2. CP 명령어는 파일을 복사하는 명령어 기존 위치에 파일을 실행하고 있어서, 덮어 씌워지면 오류가 발생

